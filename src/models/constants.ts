// ── Table Names ──────────────────────────────
export const Tables = {
  CUSTOMERS: 'customers',
  ORDERS: 'orders',
  ORDER_ITEMS: 'order_items',
  PRODUCTS: 'products',
} as const;

// ── Supabase Storage ─────────────────────────
export const StorageBuckets = {
  PRODUCT_IMAGES: 'product-images',
} as const;
